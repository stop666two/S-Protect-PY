{
  // ==========================================
  // 类型定义模块 — 所有 Enum 和 Dataclass
  // ==========================================
  // 包含 6 个 Enum、14 个 @dataclass。
  // Enum 名称和 dataclass 字段名被反射访问，不能混淆。
  // encrypt_strings 会破坏枚举值字符串匹配，必须关闭。
  // ==========================================

  obfuscate: {
    level: 2,
    rename_variables: true,
    rename_functions: false,
    rename_classes: false,
    rename_rules: { style: "hex", reserved: [] },
    encrypt_strings: false,
    encrypt_numbers: false,
    control_flow_flattening: false,
    dead_code_injection: false,
    string_split: true,
    obfuscate_imports: true,
    obfuscate_calls: true,
    obfuscate_arithmetic: true,
    obfuscate_booleans: true,
  },

  anti_debug: {
    enabled: false,
    action: "warn",
    checks: [],
  },
}
