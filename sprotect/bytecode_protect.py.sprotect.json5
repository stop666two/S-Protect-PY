{
  // ===================================================================
  // 字节码保护 — marshal + AES-GCM 加密代码对象
  // ===================================================================
  // 将 Python 源码编译为 code object，使用 marshal 序列化 + zlib 压缩
  // + AES-GCM 加密，生成 .pye 容器文件。运行时通过 SecureImporter
  // import hook 透明解密执行。
  //
  // ⚠️ 此模块的代码会生成 .pye 容器格式，因此 encrypt_strings 等
  //    可能破坏二进制格式的功能已在代码中关闭。
  // ===================================================================

  obfuscate: {
    level: 5,
  },
}
