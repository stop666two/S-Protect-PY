{
  // ==========================================
  // AST 混淆引擎 — Python 抽象语法树变换
  // ==========================================
  // 此模块是混淆器的核心，包含 Obfuscator 类、
  // _LiteralEncryptor 字符串/数字加密、不透明谓词生成等。
  // 此模块本身必须最高等级混淆。
  // ==========================================

  obfuscate: {
    level: 5,
    rename_variables: true,
    rename_functions: true,
    rename_classes: true,
    rename_rules: { style: "hex", reserved: [] },
    encrypt_strings: true,
    encrypt_numbers: true,
    control_flow_flattening: true,
    dead_code_injection: true,
    string_split: true,
    obfuscate_imports: true,
    obfuscate_calls: true,
    obfuscate_arithmetic: true,
    obfuscate_booleans: true,
  },

  anti_debug: {
    enabled: true,
    action: "exit",
    checks: ["pdb", "debugger", "vm"],
  },

  virtualization: {
    enabled: true,
    mode: "full",
  },
}
