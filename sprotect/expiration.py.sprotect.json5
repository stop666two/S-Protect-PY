{
  // ==========================================
  // 过期控制模块 - 精细化配置
  // 时间校验 + NTP 联网验证
  // ==========================================

  obfuscate: { level: 4, rename_variables: true, rename_functions: true,
    rename_classes: true, encrypt_strings: true, encrypt_numbers: false,
    control_flow_flattening: false, dead_code_injection: false },
  anti_debug: { enabled: true, action: "exit", checks: ["pdb", "debugger"] },
}
