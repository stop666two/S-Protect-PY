{
  // ==========================================
  // 启动器生成模块 — 运行时解密引擎
  // ==========================================
  // 此模块生成 _runtime/loader.py（运行时加载器）
  // 和 output/main.py（启动入口）。
  // 模板字符串中的 {} 和标识符不能被混淆/加密，
  // 因此 encrypt_strings 和 control_flow_flattening 必须关闭。
  // ==========================================

  obfuscate: {
    level: 4,
    rename_variables: true,
    rename_functions: true,
    rename_classes: true,
    encrypt_strings: false,
    encrypt_numbers: false,
    control_flow_flattening: false,
    dead_code_injection: false,
    string_split: true,
    obfuscate_imports: true,
    obfuscate_calls: true,
    obfuscate_arithmetic: true,
    obfuscate_booleans: true,
  },

  anti_debug: {
    enabled: true,
    action: "exit",
    checks: ["pdb", "debugger", "vm", "sandbox", "cuckoo", "ida"],
  },
}
