{
  // ===================================================================
  // 数字指纹 — 文件哈希校验 + 远程上报
  // ===================================================================
  // 计算所有 .pye 文件的 SHA256 指纹，支持与 integrity_manifest.json
  // 对比检测篡改/新增/缺失文件。可选远程上报到授权服务器。
  //
  // 后台定时上报线程每小时检查一次，可配置 server_url 和 customer_id。
  // ===================================================================

  obfuscate: {
    level: 5,
  },
}
